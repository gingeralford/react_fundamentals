// import React from 'react'
//above is not needed anymore, only need to do it if using class components

const Header = () => {
    return (
        <h5>My first functional component</h5>
    )
};

export default Header;